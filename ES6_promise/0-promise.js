/**
 * Returns a Promise.
 * @returns {Promise} A basic JavaScript Promise.
 */
export default function getResponseFromAPI() {
  return new Promise((resolve) => {
    // For this basic task, we just need to return an instance of a Promise.
    resolve();
  });
}
